<?xml version="1.0" encoding="utf-8"?>
<!--
  ウィジェットの「項目を追加」行をタップして開くクイック追加ダイアログ。
  Theme.SimpleMemo.QuickAdd (windowIsFloating=true) の上にカードを描く。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/quickAddTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:fontFamily="@font/inter_medium"
        android:textColor="@color/ink_secondary"
        android:layout_marginBottom="8dp"
        tools:text="買い物リストに追加" />

    <EditText
        android:id="@+id/quickAddInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="項目を入力"
        android:textSize="16sp"
        android:fontFamily="@font/inter_regular"
        android:textColor="@color/ink"
        android:textColorHint="@color/graphite"
        android:singleLine="true"
        android:imeOptions="actionDone"
        android:inputType="text"
        android:autofillHints="off"
        android:importantForAutofill="no" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/quickAddCancel"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="キャンセル"
            android:textColor="@color/ink_secondary" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/quickAddSubmit"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="追加"
            android:layout_marginStart="4dp"
            android:textColor="@color/accent_sage_dark" />
    </LinearLayout>
</LinearLayout>
